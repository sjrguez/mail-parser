# Welcome to this project


### First Steps

1. Clone this repository
2. Install all dependencies with npm install
3. Create a file named ```.env``` based on ```env.template```
4. Start the project with `npm run start:dev`
5. Access to Swagger documentation [localhost:3000/api/docs](http://localhost:3000/api/docs) 
